<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>JB's Car Lot</title>
</head>
<body>
    <h1><%= listing.year %> <%= listing.make %> - <%= listing.model %></h1>
    <ul>
        <li><a href="/home">Home</a></li>
        <li><a href="/home/new">Add Listing</a></li>
        <li><a href="/home/<%= listing.id %>/edit">Edit Posting</a></li>
        <li style="float: right;"><a href="/logout">Log Out</a></li>
    </ul>
    <h3>People Watching: <%=listing.watching %></h3>
    <form action="/home/<%= listing._id %>?_method=DELETE" method="POST">
        <input type="submit" value="REMOVE">
    </form>
    <img src="<%= listing.img %>" alt="vehicle">
    <p>Description: <%= listing.description %></p>
    <p>Mileage: <%= listing.mileage %></p>
    <p>Price:
        <% if(listing.price === 0) { %> Contact Seller
        <% } else { %>
        $<%= listing.price %>
        <% } %>
    </p>
    <% if(listing.price ===0) {} else { %>
    <form action="/home/<%= listing._id %>/bid?_method=PUT" method="POST">
        <input type="submit" value="BID">
    </form>
        <% } %>
    <form action="/home/<%= listing._id %>/watch?_method=PUT" method="POST">
        <input type="submit" value="WATCH">
    </form>
</body>
</html>